# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jobtracker
DB_USER=postgres
DB_PASSWORD=postgres

# Backend server
SERVER_PORT=8080

# JWT
# Must be at least 32 characters — change this in production!
JWT_SECRET=supersecretkeythatmustbeatleast32characterslong
